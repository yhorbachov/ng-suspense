import { SuspenseDirective } from './suspense.directive';

describe('SuspenseDirective', () => {
  it('should create an instance', () => {
    const directive = new SuspenseDirective();
    expect(directive).toBeTruthy();
  });
});
