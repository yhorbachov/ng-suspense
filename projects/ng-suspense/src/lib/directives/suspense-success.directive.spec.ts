import { SuspenseSuccessDirective } from './suspense-success.directive';

describe('SuspenseSuccessDirective', () => {
  it('should create an instance', () => {
    const directive = new SuspenseSuccessDirective();
    expect(directive).toBeTruthy();
  });
});
